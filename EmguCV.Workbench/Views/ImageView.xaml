<UserControl x:Class="EmguCV.Workbench.Views.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:EmguCV.Workbench.ViewModels" 
             mc:Ignorable="d" 
             Width="640"
             Height="500">
    <UserControl.DataContext>
        <vm:ImageViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Canvas Name="ImageCanvas" Grid.Row="0" Width="640" Height="480">
            <Image Source="{Binding Image}" Width="{Binding Width, ElementName=ImageCanvas}" Height="{Binding Height, ElementName=ImageCanvas}" />
            <TextBlock Canvas.Right="5" Canvas.Bottom="3" FontSize="12" Foreground="Red" Text="{Binding FrameInfo}" />
        </Canvas>

    </Grid>
</UserControl>
